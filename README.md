# Jumpshell

Jumpshell is a multi-surface automation repo with:

- PowerShell module sources in `src/pwsh`
- Python backend sources in `src/python`
- VS Code/Cursor extension sources in `extensions/jumpshell`
- skill sources in `skills`
- MCP template assets in `mcps`

## Quick Start

### 1. Import module from repository checkout

```powershell
Import-Module .\Jumpshell.psd1 -Force
```

### 2. Build and install extension (optional)

```powershell
pwsh ./extensions/Build.ps1 -Install
```

### 3. Configure MCP

```powershell
Install-JumpshellMcp -Scope User
Get-JumpshellMcp
```

Or run extension command:

- `Jumpshell: Install MCP Configuration`

## Important Entry Points

| Entry point | Purpose |
|---|---|
| `Jumpshell.psd1` | Root module shim to `src/pwsh/Jumpshell.psm1` |
| `Jumpshell.psm1` | Root compatibility module shim |
| `Install.ps1` | Root extension install workflow wrapper |
| `src/pwsh/Install.ps1` | Module dependency/skills/apps/MCP installer |
| `extensions/Build.ps1` | Build VSIX (optionally install) |
| `extensions/Install.ps1` | Install existing VSIX into active editor |

## Repository Layout

```text
.
|- README.md
|- docs/
|- src/
|  |- pwsh/
|  |  |- Jumpshell.psm1
|  |  |- Install/
|  |  |- mcp/
|  |  |- vscode/
|  |  `- vscode-workspaces/
|  `- python/
|     `- ai-backends/
|- extensions/
|  `- jumpshell/
|- skills/
`- mcps/
```

## Documentation

Start with:

- [docs/README.md](docs/README.md)

Jekyll docs site (dark theme with autogenerated nav):

```powershell
cd ./docs
bundle install
bundle exec jekyll serve
```

Automatic deployment to GitHub Pages is configured in `.github/workflows/docs-pages.yml`.

Key guides:

- [docs/pwsh/Repository-Layout.md](docs/pwsh/Repository-Layout.md)
- [docs/pwsh/PowerShell-Module.md](docs/pwsh/PowerShell-Module.md)
- [docs/pwsh/MCP-Server.md](docs/pwsh/MCP-Server.md)
- [docs/extension/VSCode-Extension.md](docs/extension/VSCode-Extension.md)
- [docs/extension/VSCode-ChatSessions.md](docs/extension/VSCode-ChatSessions.md)
- [docs/extension/VSCode-WorkspaceManagement.md](docs/extension/VSCode-WorkspaceManagement.md)
- [docs/extension/VSCode-QuickReference.md](docs/extension/VSCode-QuickReference.md)
- [docs/ai/AI-Backends.md](docs/ai/AI-Backends.md)

## Notes on the Recent Refactor

- Canonical module scripts moved from repo root to `src/pwsh`.
- Root module files remain as compatibility shims.
- Root `Install.ps1` now delegates to extension install flow.
- MCP runtime and installer scripts now live only in `src/pwsh/mcp`.
