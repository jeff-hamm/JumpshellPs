---
applyTo: "**"
---
# Global Prompts
- Use `git-workflow.prompt.md` when I ask for commit or copilot-branch help
- For workspace-level customizations, prefer `.agents/` over `.copilot/` or `.github/`
- For global file edits, prefer user-profile skills: `/setting`, `/rule`, `/create-prompt-global`, `/create-skill-global`
